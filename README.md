# test-s3-infra-7

**App:** test-s3
**Requirements:** create an s3 bucket

Auto-generated by [InfraAgent](https://github.com).

## Authentication

This repo uses **OIDC** to authenticate with AWS — no static keys stored anywhere.

Secrets are set automatically by InfraAgent:

| Secret | Value |
|--------|-------|
| `AWS_ROLE_ARN` | IAM role ARN (set automatically) |
| `AWS_REGION`   | AWS region (set automatically) |

**Actions → Deploy Infrastructure → Re-run jobs** to redeploy.
